# Android Release Signing Configuration
# =====================================
# 
# INSTRUCTIONS:
# 1. Generate a keystore (run this command once):
#    keytool -genkey -v -keystore ~/bookbed-release-key.jks -keyalg RSA -keysize 2048 -validity 10000 -alias bookbed
#
# 2. Copy this file to key.properties (without .example):
#    cp android/key.properties.example android/key.properties
#
# 3. Fill in your actual values below
#
# 4. NEVER commit key.properties to git (it's already in .gitignore)
#
# =====================================

storePassword=YOUR_KEYSTORE_PASSWORD
keyPassword=YOUR_KEY_PASSWORD
keyAlias=bookbed
storeFile=/Users/YOUR_USERNAME/bookbed-release-key.jks
