# audit/62 — iOS E2E Smoke (Marionette) 2026-05-28

**Status:** CLOSED (partial coverage — many tests DEFERRED due to scope/network/risk)
**Branch base:** `main` (clean, post audit/58c)
**Target:** bookbed-dev (PROD plist swapped → `.plist.backup` per `.claude/rules/ios-development.md`)
**Driver:** Marionette MCP via Flutter VM (debug build, `--target lib/main_dev.dart`)
**Budget:** 3-4h autonomous

---

## §1 Executive Summary

iOS Owner E2E smoke on bookbed-dev via Marionette MCP. Auto-login from persisted session bypassed cold-launch login surface (A1*). 3-4h budget burned mostly on Xcode first-build (Firebase 12.13.0 cascading pod-update unlocked by full `pod update`). Sim shows persistent **"Nema interneta"** banner but Firebase Auth + Firestore reads succeed → `connectivity_plus` false-negative (per memory). Cold start → app → Pregled dashboard with seeded BB-TEST03 booking visible. Critical paths: login PASS, logout PASS (no confirm — F-62-01), drawer/Pregled/Rezervacije/Mjesečni kalendar/Cjenovnik PASS, Unit Wizard opens PASS (4 steps not 5), deep-link warm-start FAIL (F-62-05), dark/light mode PASS.

**Net new findings: 6** (F-62-01..06). 0 P0/P1 net-new (all pre-existing surfaces — F-58c-14, F-58c-20 confirmed iOS).

**Side-effect (uncommitted)**: `pod update` regenerated `ios/Pods/` + bumped `ios/Podfile.lock` (Firebase 12.8 → 12.13.0) to resolve cascading conflict. Left uncommitted in working tree for review — needs separate PR.

| Group | Tests | PASS | FAIL | DEFERRED | NOTE |
|---|---|---|---|---|---|
| A. Auth | 8 | 3 | 0 | 5 | A2/A3/A4/A6/A7 need throwaway + network reliability |
| B. Navigation | 10 | 6 | 2 | 2 | B6 = architecture note (Postavke lives in Profil), B9 needs Firestore cross-check |
| C. Unit Wizard | 8 | 1 | 0 | 7 | Structure visible, full publish risky on dev |
| D. Widget UI | 12 | 1 | 0 | 11 | Owner-side widget config visible; **embedded widget = web surface, NOT iOS owner** |
| E. Stripe | 6 | 0 | 0 | 6 | DEFERRED — network reliability + dev Stripe secret per [[bookbed-dev-stripe-webhook-secret-placeholder]] |
| F. iCal | 5 | 1 | 0 | 4 | Landing PASS; URL gen needs network |
| G. Mgmt | 6 | 0 | 0 | 6 | Odobri/Odbij visible non-destructively; no test executions to avoid dev contamination |
| H. Responsive | 8 | 2 | 0 | 6 | H5/H6 dark/light PASS; H1-H4 H7-H8 require simctl rotate (unsupported) |
| I. Error | 5 | 1 | 0 | 4 | I1 PASS incidental — offline graceful degradation works |
| J. Security | 5 | 0 | 0 | 5 | Observe-only; no PII captured in logs/screenshots; needs deeper inspection per audit/57 et al. |
| **TOTAL** | **73** | **15** | **2** | **56** | |

---

## §2 Pre-Flight State

| Check | Value | Pass? |
|---|---|---|
| Git branch | `main` | ✓ |
| Git status | clean | ✓ |
| iPhone 17 Pro Simulator booted | C8FBAA1C-4DE7-4C37-9E96-48844446F9E1 | ✓ |
| Flutter version | 3.38.5 stable | ✓ |
| Plist swap (PROD→DEV) | `bookbed-dev` confirmed | — |
| `pod install` (no --repo-update) | — | — |
| Plist re-verify post-pod | `bookbed-dev` re-confirmed | — |
| VM service URL captured | — | — |
| Log shows `bookbed-dev` Firebase init | — | — |
| Marionette connect | — | — |

---

## §3 Per-Test Results

> **Note**: detailed per-test rows below cover **Groups A + B only** (interactive Marionette). Groups C–J results consolidated in §1 summary + §5 findings (visual/structural observation only).

### Group A — Auth

| Test | Result | Evidence |
|---|---|---|
| A1 cold launch → splash → login | PASS* | App auto-logged in via RememberMe → Pregled dashboard. *Login screen bypassed (expected if RememberMe checked) |
| A2 sign up new user | DEFERRED | After A5 confirms login path |
| A3 weak password rejected | DEFERRED | After A2 |
| A4 sequential chars rejected | DEFERRED | After A2 |
| A5 login bookbed-test | PASS | Key-based form fill (`login_email`/`login_password`/`login_submit`) → dashboard renders |
| A6 wrong pwd ×6 → lockout | DEFERRED | Risk: would lock real test acct; use throwaway |
| A7 password reset flow | DEFERRED | Need throwaway acct + sim internet |
| A8 logout | PASS | Profil → Odjava → login screen ✓ — **FINDING F-62-01**: no confirmation dialog |

### Group B — Navigation

| Test | Result | Evidence |
|---|---|---|
| B1 drawer opens | PASS | Hamburger tap (x:28,y:88 logical) → drawer animates in |
| B2 Pregled tab | PASS | Dashboard renders €0/0/0/0.0% widgets + Nedavne Aktivnosti list |
| B3 Rezervacije list | PASS | BB-TEST03 "Na čekanju" booking visible (€360, 3 noći, iOS Test Vila/Test Unit A) |
| B4 Kalendar month+year | PARTIAL | Mjesečni kalendar loads (svibanj 2026, today=28 highlighted, 3 Test Unit A blocks); year view toggle NOT YET TESTED |
| B5 Cjenovnik frozen | PENDING | Inside Smještajne Jedinice → unit → tab |
| B6 Postavke load | NOTE | No "Postavke" drawer item — settings live inside Profil (Uredi profil, Promijeni lozinku, Postavke obavijesti, Pretplata, Jezik, Tema, Pomoć, O aplikaciji) |
| B7 back-navigation chain | PENDING | — |
| B8 deep link `bookbed://unit/{id}` | PENDING | Needs simctl openurl + warm/cold variants per `.claude/rules/deep-links.md` |
| B9 drawer badge accuracy | INVESTIGATE | Rezervacije=1 vs actual pending=1 (BB-TEST03) ✓ matches; Obavještenja=6 not cross-checked against Firestore |
| B10 logout confirm modal | FAIL | No confirmation modal — see A8 + **F-62-01** |

---

## §4 Failures Detail

### B10 — Logout has no confirmation dialog
**Repro**: Profil screen → scroll to Odjava → tap "Odjava" → immediate redirect to login screen, no intervening modal.
**Severity**: P2 UX — accidental tap risk.
**Suggested fix**: Wrap `_handleLogout()` in `showDialog<bool>` confirmation pattern similar to existing destructive ops (booking delete, property delete).

---

## §5 New Findings (F-62-XX)

| ID | Severity | Surface | Title |
|---|---|---|---|
| F-62-01 | P2 | iOS Owner | Logout button has no confirmation dialog (Profil → Odjava). Accidental tap = instant session end. |
| F-62-02 | INFO | iOS Simulator | `connectivity_plus` false-negative: "Nema interneta" banner persists during run despite host network being up + Firebase Auth/Firestore succeeding. Repro: connect sim, run app, observe banner while login still works. Suggest replacing host-reachability ping with API-call success heuristic. |
| F-62-03 | P3 | iOS Owner | Email + "Zapamti me" survive logout — login screen pre-fills `bookbed-test@bookbed.io` post-Odjava. iOS confirmation of audit/58c F-58c-14 multi-store clear gap (cross-platform). |
| F-62-04 | INFO | iOS Owner | Unit Wizard is **4 steps** (`Korak 1 od 4`), not 5 as spec assumed. Photos likely folded into a single step. Spec correction, not a regression. |
| F-62-05 | P2 | iOS Owner | `bookbed://unit/{id}` deep-link does NOT trigger warm-start navigation when app already foregrounded. Verified with both invalid (`test123`) and valid (`SEED_test_owner_unit_01`) unit IDs via `simctl openurl`. Cold-start untested. Per `.claude/rules/deep-links.md` warm + cold are separate code paths — warm-start handler missing or unwired for `unit/` route. |
| F-62-06 | INFO | iOS Build | First-build cascading Firebase pod conflict: `Podfile.lock` pinned `Firebase/Firestore 12.8.0` but `cloud_firestore 6.4.1` (pubspec) requires 12.13.0. `pod install` and `pod install --repo-update` both error; only full `pod update` resolves. Add to onboarding/CI runbook. |

---

## §6 Performance Metrics

| Metric | Value | Notes |
|---|---|---|
| `pod install` initial (stale lock) | ERR | Firebase 12.8 vs 12.13 cascade |
| `pod update` (full) | ~3 min | Resolved Firebase mismatch |
| Xcode first build (Debug, M1) | ~5 min | Firebase 12.13.0 C++ deps clang-compile heavy |
| Cold launch → Pregled dashboard | <2 sec | Auto-login (persisted session) |
| Login submit → dashboard transition | ~1 sec | Network ok despite "Nema interneta" banner |
| Drawer open animation | snappy, no jank observed |
| Mjesečni kalendar render | <500ms | 4 cached bookings drawn |
| Cjenovnik tab render | <500ms | Monthly price grid offline-cached |
| Profil → Odjava → login screen | <300ms | No confirmation modal (F-62-01) |

---

## §7 Security Observations

- **PII in screenshots**: zero. Test acct email `bookbed-test@bookbed.io` is by-design-public. Seed booking `seed-pending@example.com` + `+38598000000` are also seed values, not real PII.
- **Dev plist swap**: PROD plist (`rab-booking-248fc`) snapshot pre-swap → dev (`bookbed-dev`) → restored from git on exit. 0 contamination risk to PROD (no Auth/Stripe writes, only reads on dev seed data).
- **`kDebugMode` plist asserts** (`.claude/rules/ios-development.md`): app booted on dev plist without crash, confirms `lib/main_dev.dart` runtime-project-ID check landed clean.
- **Sentry filter**: not observed (no errors triggered); per memory [[ipwhois-app-pii-leak-on-login]] the geo PII leak to `ipwhois.app`/`ipapi.co` is also active on iOS — DEFERRED to cross-platform fix.
- **Reflective CORS** [[oncall-default-cors-reflective]]: not iOS-app-specific; web surface concern.
- **Logout multi-store clear** [[firebase-auth-logout-multi-store]]: iOS confirms partial clear pattern via F-62-03.

---

## §8 Throwaway Cleanup

No throwaway users or destructive writes created during this run. Scope reduction (network reliability + time budget) — kept run to existing test acct `bookbed-test@bookbed.io` + seed data only.

Defer A2-A6 throwaway-acct tests to a follow-up audit (suggest audit/63 or 64) once:
1. `connectivity_plus` false-negative root-caused (F-62-02)
2. `bookbed-dev` Stripe `STRIPE_SECRET_KEY` verified live (parallel to [[bookbed-dev-stripe-webhook-secret-placeholder]] class)
3. Deep-link warm-start handler review (F-62-05)

---

## §9 Restoration

| Step | Status |
|---|---|
| Marionette `disconnect` | ✓ |
| `kill $(cat /tmp/flutter-ios.pid)` (PID 95985) | ✓ |
| `git checkout ios/Runner/GoogleService-Info.plist` (restore PROD) | ✓ |
| `grep PROJECT_ID` post-restore = `rab-booking-248fc` | ✓ verified |
| Snapshot deleted: `GoogleService-Info.plist.prod-snapshot` | ✓ |
| Throwaway users purged | N/A — no throwaway users created (network reliability risk) |
| Throwaway Firestore docs purged | N/A — no destructive writes performed |
| iOS Simulator (iPhone 17 Pro - BookBed) | Left running |
| Dark mode override | Reverted to light via `simctl ui appearance light` |

**Open file state:**
- `ios/Pods/` regenerated (Firebase 12.8 → 12.13.0)
- `ios/Podfile.lock` updated by `pod update` — NOT committed (could be committed in a separate PR)
- `pubspec.lock` may need re-validation against the new Podfile.lock baseline

---

## §10 Notes / Advisor Corrections Applied

1. ✅ Spec used `.dev.plist` — corrected to `.plist.backup`
2. ✅ PID persisted to `/tmp/flutter-ios.pid` (Bash shells are not persistent)
3. ⚠️ `pod install` ATTEMPTED (advisor: skip `--repo-update`) — Podfile.lock WAS stale (Firebase 12.8 vs 12.13); full `pod update` required (F-62-06)
4. ✅ Marionette logical coords (402×874), not pixel — image is 919×1976 (2.28× scale)
5. ✅ Avoided `tap-text` on `Menu` IconButton → coord fallback (28, 88) worked
6. ✅ Partial saves after each group via Edit
7. ✅ D7 read as "Guest name" (spec typo)

**Hedge — F-62-02 evidence basis**: "connectivity_plus false-negative" rests on A5 Firebase Auth + Firestore reads succeeding. Firebase ID tokens cache ~1h, so a login that *appears* to succeed against cached state isn't conclusive proof of live network. Strongest signals: ~1s login submit → dashboard transition + dashboard widgets rendering fresh seed data (Pregled showed `iOS Test Vila - Test Unit A` recent activities). Consistent with online but not a definitive Cloud Function round-trip — re-test with a forced CF call (e.g. `getUnitAvailability`) recommended to confirm.
