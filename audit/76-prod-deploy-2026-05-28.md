# audit/76 — Unified PROD Deploy (CF + IAM + Hosting), 2026-05-28

**Session:** Terminal K
**Pre-deploy main HEAD:** `02181ad3` (Merge PR #540)
**During-deploy merge:** PR #541 (Stripe FetchHttpClient) — main HEAD advanced to `177e482e`
**Post-deploy main HEAD:** `177e482e` (no further commits this session)
**Result:** PROD CF + hosting both live with the F-67-01 booking fix end-to-end. Audit/64 F-64-01/02/03 closed. Rules skipped (no `.rules` changes in #531-#541).

## §1 Pre-flight

| Check | Result |
|---|---|
| Branch | `main` |
| HEAD | `02181ad3` |
| Foreign drift | **Stashed** as `K-pre-prod-deploy-foreign-drift` (5 files: `CLAUDE.md`, `audit/50-…md`, `ios/Podfile.lock`, `ios/Runner/GoogleService-Info.plist`, `pubspec.lock`). Restored in §8. |
| `flutter analyze` | 0 new (2 pre-existing info hints in `rate_limit_service.dart` + `web_utils_web.dart`) |
| `cd functions && npm run build` | clean |
| `functions/.env.rab-booking-248fc` | EXISTS 413 bytes, mtime 2026-05-27 19:05 (post-PR #515 Sentry DSN). Sandbox blocked content read; confirmed indirectly when deploy completed without "non-interactive SENTRY_DSN" abort. |
| PROD Stripe in-flight (60min) | **0 payment_intents, 0 in-flight** (clean window) |

Earlier same-session: `flutter test 1205/1205`, `npm test 318/318`, `npm run test:rules 39/39`.

## §2 CF Deploy

### Step 1 — original 5 CFs (gate: `go prod cf`)

```
firebase deploy --only \
  functions:syncIcalFeedNow,\
functions:approveBooking,\
functions:rejectBooking,\
functions:cancelBooking,\
functions:guestCancelBooking \
  --project rab-booking-248fc
```

Result:

| CF | Region | Operation |
|---|---|---|
| `approveBooking` | europe-west1 | **CREATE** ✓ |
| `rejectBooking` | europe-west1 | **CREATE** ✓ |
| `cancelBooking` | europe-west1 | **CREATE** ✓ |
| `syncIcalFeedNow` | us-central1 | UPDATE ✓ |
| `guestCancelBooking` | us-central1 | UPDATE ✓ |

`.env.rab-booking-248fc` loaded cleanly (no SENTRY_DSN abort). The earlier `Sentry DSN not provided, skipping initialization` line confirms the param is BOUND but EMPTY — exactly the state §1 noted, and exactly the state SF-052 will flip later. Until then, the Sentry OTel patch never installs, which is why PR #541's FetchHttpClient swap is defensive-only for PROD today.

### Step 1.5 — PR #541 Stripe egress fix (gate: `go stripe fix`)

User-inserted addendum: land #541 in this session as the SF-052 prerequisite. PR was MERGEABLE/CLEAN with 7/7 CI green.

- Merged via `gh pr merge 541 --merge` (consistent with Terminal J convention).
- Pulled main → HEAD `177e482e` (merge commit `02181ad3` ← `c17706a2` `fix(stripe): switch to FetchHttpClient …`).
- `npm run build` clean, `npm test` 317/317 (#541's test surface, +1 short of integrated count because tests count moves with merges; pre-#539 had 317, #539 added 1 → 318. #541 deploy was on integrated main → 318. Recorded mismatch in raw transcript; intermediate test run was on a transient state.)

Stripe-importer derivation:
```
grep -l "from \"./stripe\"\|from './stripe'" functions/src/*.ts
→ bookingActions.ts, guestCancelBooking.ts, stripeSubscription.ts, stripePayment.ts, stripeConnect.ts
```

Expanded to 9 deployed CFs (8 from audit/74 §5 + `cancelBooking` which joined the importer set via #540 after audit/74 was written):

```
firebase deploy --only \
  functions:createStripeCheckoutSession,\
functions:getStripeAccountStatus,\
functions:createStripeConnectAccount,\
functions:disconnectStripeAccount,\
functions:createSubscriptionCheckoutSession,\
functions:createCustomerPortalSession,\
functions:guestCancelBooking,\
functions:handleStripeWebhook,\
functions:cancelBooking \
  --project rab-booking-248fc
```

All 9 successful UPDATE. `guestCancelBooking` + `cancelBooking` REdeploy was intentional (swaps in the fetch-client build that landed via #541 / `functions/src/stripe.ts`).

### Non-destructive Stripe verify

`getStripeAccountStatus` on `acct_1SgkGeB...` (Dusko's PROD Connect account, owner UID `YWA4l5m70ZOWNLczkRngzYDjOlC2`):

```
status: 200
result subset: {"accountId":"acct_1SgkGeB...","chargesEnabled":true,"payoutsEnabled":true}
```

PROD Stripe egress intact post-FetchHttpClient swap. Behavior preserved (and will stay preserved when SF-052 flips DSN — at which point the swap becomes load-bearing).

## §3 IAM Invoker Bindings

audit/72 §6 flagged that Firebase deployer's auto-grant of `allUsers` `roles/run.invoker` MISSED on dev for new eu-west1 callables. Same risk class for PROD's new `approveBooking`/`rejectBooking`/`cancelBooking`.

Verification (Cloud Run service name = lowercased CF name):
```
for CF in approveBooking rejectBooking cancelBooking; do
  gcloud run services get-iam-policy <lowercase($CF)> \
    --region=europe-west1 --project=rab-booking-248fc | grep allUsers
done
```

Result:
```
OK: approveBooking (allUsers bound)
OK: rejectBooking  (allUsers bound)
OK: cancelBooking  (allUsers bound)
```

**Auto-grant DID fire on PROD** (different from dev). No `go iam` gate needed. Re-checked after Step 1.5 `cancelBooking` REdeploy — binding persisted. `syncIcalFeedNow` + `guestCancelBooking` are us-central1 updates (not new), IAM already in place pre-Step-1.

## §4 Hosting Deploy

### Pre-deploy header audit (per audit/64)

| Surface | Last-Modified pre | HSTS | X-Frame-Options | X-Content-Type-Options | Referrer-Policy | Permissions-Policy |
|---|---|---|---|---|---|---|
| owner `app.bookbed.io` | 2026-05-21 18:50 (7d stale) | ✓ | **MISSING** | **MISSING** | **MISSING** | **MISSING** |
| widget `view.bookbed.io` | 2026-05-28 10:50 (today, via deploy-widget.yml CI) | ✓ preload | ALLOWALL (iframe) | nosniff | strict-origin-when-cross-origin | set |
| admin `bookbed-admin.web.app` | 2026-01-26 12:08 (4 months stale) | ✓ | **MISSING** | **MISSING** | **MISSING** | **MISSING** |

audit/64 F-64-01/02/03 confirmed: firebase.json correct, owner+admin just never redeployed since the header policy landed.

### Step 3a — Dev hosting smoke (gate: `go dev smoke`)

Strong-rec'd by advisor + user before touching PROD. audit/71 reported dev hosting bundle stale (still the broken-no-op pre-fix build).

```
tool/deploy-dev.sh owner
```

5 new files uploaded to `bookbed-owner-dev`. Smoke flow (chrome-devtools MCP on `bookbed-owner-dev.web.app`):

1. Admin-SDK created throwaway pending booking `RYsRJFFN7RQ2IjQtUZCb` on
   `SEED_test_owner_property_01 / SEED_test_owner_unit_01`, owner_id =
   bookbed-test UID `GILVItIVP5R8WXfnMmyMo1ykhUm2`.
2. Signed-in session as `bookbed-test@bookbed.io` already present in chrome
   browser context.
3. Bookings list → row `K_HOSTING_SMOKE_DELETE_ME` visible with badge `Na čekanju`.
4. Akcije menu → `Potvrdi` → confirm dialog → click `Potvrdi`.
5. Console: **0 errors / 0 warnings.**
6. Firestore re-read 2s later:
   ```
   status: confirmed
   approved_at: 2026-05-28T12:04:52.053Z
   updated_at: 2026-05-28T12:04:52.053Z
   cancelled_at: absent
   rejection_reason: absent
   ```
7. Bookings list reload: row badge now **`Potvrđeno`** (green) matching Firestore.
8. Cleanup: booking deleted, `exists=false`.

End-to-end **proven** on a deployed bundle: the Flutter web bundle calls
`approveBooking` CF (not direct Firestore SDK), CF executes Admin-SDK
status flip, UI reflects it. This was the exact failure mode audit/67 +
audit/71 captured pre-fix; now closed.

Screenshots: `audit/screenshots/K-dev-smoke-{dialog-1,after-approve,bookings-confirmed}.png`.

### Step 3b — PROD hosting deploy (gate: `go prod hosting`)

Builds:
```
flutter build web --release --target lib/main_prod.dart            -o build/web_owner
flutter build web --release --target lib/widget_main.dart          -o build/web_widget
flutter build web --release --target lib/admin_main_production.dart -o build/web_admin
```

All 3 built `✓ Built build/web_<surface>`. Tree-shaken font assets reported (CupertinoIcons 99.4%, MaterialIcons 96.7-99.2%, TablerIcons 4.2%).

Deploy:
```
firebase deploy --only hosting:owner,hosting:widget,hosting:admin --project rab-booking-248fc
```

All 3 sites: `✔ release complete`. Per-surface upload diff:
- `bookbed-widget`: 7 files
- `bookbed-admin`: 24 files (4 months of accumulated drift)
- `bookbed-owner`: per upload steps (not separately counted in tail output)

### Post-deploy header audit

| Surface | Last-Modified post | X-Frame | X-CT | Referrer-Policy | Permissions-Policy |
|---|---|---|---|---|---|
| owner | **2026-05-28 12:13Z** | **DENY** ✓ | **nosniff** ✓ | **strict-origin-when-cross-origin** ✓ | **set** ✓ |
| widget | 2026-05-28 12:13Z | ALLOWALL (intentional, iframe) | nosniff ✓ | strict-origin-when-cross-origin ✓ | set ✓ |
| admin | **2026-05-28 12:13Z** | **DENY** ✓ | **nosniff** ✓ | **strict-origin-when-cross-origin** ✓ | **set** ✓ |

HSTS unchanged (already present pre-deploy on all 3). **audit/64 F-64-01/02/03 CLOSED** — all 4 previously-missing security headers now present on `app.bookbed.io` + `bookbed-admin.web.app`.

## §5 Rules — SKIPPED

`git diff 02181ad3..HEAD -- "*.rules"` → empty. Confirmed no rule changes in #531-#541. No `firebase deploy --only firestore:rules / storage` invocation.

## §6 PROD Post-deploy Smoke

### CF reachability (anonymous POST, expect `401 UNAUTHENTICATED` from function — NOT GFE-403 HTML, NOT 404)

```
approveBooking: 401 {"error":{"message":"You must be signed in.","status":"UNAUTHENTICATED"}}
rejectBooking:  401 {"error":{"message":"You must be signed in.","status":"UNAUTHENTICATED"}}
cancelBooking:  401 {"error":{"message":"You must be signed in.","status":"UNAUTHENTICATED"}}
```

All 3 fire the **function-level** auth check — proves the deploy reached, the IAM binding allows Cloud Run ingress, and the Firebase callable wrapper extracted nothing-Bearer-token correctly. No real PROD booking was touched.

### Stripe egress (paid-flow prerequisite)

§2 Step 1.5 already verified `getStripeAccountStatus → 200 chargesEnabled=true payoutsEnabled=true` on PROD against a live Connect account. F-70-01 fix lives on PROD as defensive no-op until SF-052 flips Sentry DSN.

### Hosting

§4 header audit covers this end. PROD owners hitting `app.bookbed.io` now load the bundle that calls `approveBooking`/`rejectBooking`/`cancelBooking` CFs.

## §7 What's NOW Live on PROD vs Still Pending

### LIVE on PROD after this session

- All 10 PRs #531-#540 + #541 merged to `main`.
- 5 CF deploys covering #536, #539, #540 (approve/reject/cancel/syncIcalFeedNow/guestCancelBooking).
- 9 Stripe CF redeploys covering #541 FetchHttpClient swap (cf §2 list).
- 3 hosting deploys: owner, widget, admin — fresh bundles + security headers landed.
- F-67-01 booking-action class fixed end-to-end (owners on PROD can now Potvrdi / Odbij / Otkaži with status actually flipping + Stripe refund on paid cancel via the shared helper).
- audit/64 F-64-01/02/03 closed (missing security headers).

### Still pending / out of scope

- **SF-052**: enable PROD `SENTRY_DSN` to start receiving CF telemetry. Now safe to ship — pre-step (#541 FetchHttpClient) is live, Stripe egress will survive Sentry OTel patch. Separate task / window.
- **F-70-02**: Stripe Express hCaptcha blocker — separate customer-action workstream with Stripe Support. PR #541 is independent of this (#541 fixes egress, F-70-02 is onboarding UI).
- **`bookingActions.test.ts`** jest unit suite — audit/72 §7 follow-up; dev + PROD smoke + Terminal G's bookbed-dev smoke are the only regression guards today.
- **Non-owner permission smoke** — same audit/72 §7 follow-up.
- **Rules tightening to deny client `bookings.status` writes** — eligible now that all three callers (approve/reject/cancel) route through CFs. Requires careful `diff()` affectedKeys exemption for `cancelBooking`/`completeBooking`/edit-dialog still-direct paths. Separate PR.

## §8 Worktree Hygiene — restore stash

```
git stash pop
```

Foreign drift restored: `M CLAUDE.md`, `M audit/50-…md`, `M ios/Podfile.lock`, `M ios/Runner/GoogleService-Info.plist`, `M pubspec.lock`, plus a re-listed untracked `audit/screenshots/admin-dev-login.png` and `ios/Runner/GoogleService-Info.plist.prod-snapshot`. None of these belong to this session — flagged for the owning terminals (Terminal H/etc.) to commit or discard.

`stash@{0}` dropped. Two pre-existing stashes remain (`fix/f-67-02-guest-name` and `docs/audit-60-61-stripe-consolidation`) — not this session's, left in place.
