PROMPT 8: Property Cards & Search Results

Implementiraj PropertyCard widget i SearchResultsScreen:

1. `lib/shared/widgets/property_card.dart`:

   Dizajn (inspiracija: thethinkingtraveller.com):
   - Image carousel sa dots indicator
   - Property name (bold, 18px)
   - Location sa icon
   - Price (per night, prominent)
   - Quick info icons (guests, bedrooms, bathrooms)
   - "View Details" button sa hover effect
   - Favorite icon (top-right)

   Implementiraj:
   - Hero animation za sliku
   - Skeleton loader dok se učitava
   - Error handling za slike
   - Responsive sizing

2. `lib/features/search/presentation/screens/search_results_screen.dart`:

   Layout:
   - Sticky AppBar sa search summary
   - Filter bar (Sort by: Price, Rating, etc.)
   - Side panel sa advanced filters (desktop)
   - Bottom sheet sa filterima (mobile)
   - Results grid (2 cols mobile, 3-4 cols desktop)
   - Infinite scroll sa pagination
   - Results count indicator
   - Grid/List toggle view
   - Empty state (no results)

3. FILTER PANEL (`filter_panel_widget.dart`):
   - Price range slider
   - Property type (villa, apartment)
   - Amenities (checkboxes)
   - Number of bedrooms/bathrooms
   - "Apply Filters" button
   - "Clear All" button

   Responsive:
   - Side panel za desktop
   - Bottom sheet za mobile

4. STATE MANAGEMENT:
   - SearchNotifier (Riverpod)
   - Filter state provider
   - Results pagination provider
   - SearchProvider (query params, filters)
   - SearchResultsProvider (fetch properties sa filtrima)

Sve sa smooth animations i loading states.

5. OPTIMIZACIJE:
   - Debounce search input
   - Cache rezultata
   - Image lazy loading sa cached_network_image

DELIVERABLES:
- Reusable PropertyCard widget
- Search results screen sa grid/list view
- Filter bottom sheet
- Riverpod providers za search i filter state
- Infinite scroll implementation
- Empty states i error handling
