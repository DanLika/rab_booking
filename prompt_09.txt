PROMPT 9: Property Details Screen - Desktop & Mobile

Kreiraj kompleksnu PropertyDetailsScreen sa responsive layoutom:

1. `lib/features/property/presentation/screens/property_details_screen.dart`:

   DESKTOP LAYOUT (2 stupca):
   - Left: Content (70%)
   - Right: Booking widget (30%, sticky)

   MOBILE LAYOUT:
   - Scroll view sa svim sadržajem
   - Floating action button: "Rezerviraj" (sticky bottom)

2. KOMPONENTE:

   a) IMAGE GALLERY (`image_gallery_widget.dart`):
      - Main image + thumbnail grid
      - Lightbox view sa swipe
      - Zoom functionality
      - Hero image (large)
      - Thumbnail grid (4-6 slika)
      - "View all photos" button → opens full-screen gallery
      - Pinch-to-zoom, swipe gestures

   b) PROPERTY INFO SECTION:
      - Title, location, rating
      - Quick facts icons
      - Full description (expandable)
      - Amenities grid sa ikonama
      - Property name (heading)
      - Location sa map preview
      - Quick stats (guests, bedrooms, bathrooms, sqm)

   c) LOCATION MAP:
      - Static map placeholder (ili Google Maps embeded)
      - Address details
      - Embedded Google Maps
      - Nearby attractions markers

   d) HOST INFO:
      - Profile picture, name
      - "Contact Host" button
      - Host info (avatar, name, "Superhost" badge)

3. CONTENT SECTIONS (Left column):

   a) DESCRIPTION:
      - Full text sa "Read more" expansion
      - Amenities grid sa icons

   b) UNITS SECTION:
      - List/Grid svih unitova u propertyu
      - Svaki unit ima: ime, sliku, cijenu, capacity
      - "Select" button → otvara booking flow

   c) AVAILABILITY CALENDAR:
      - table_calendar widget
      - Highlight blocked dates
      - Allow selecting check-in/check-out

   d) REVIEWS SECTION:
      - Average rating sa star display
      - Individual review cards
      - "Show all reviews" pagination

4. BOOKING WIDGET (`booking_widget.dart`) (Right column / Bottom FAB):

   LAYOUT:
   - Price display (per night + total)
   - Date range picker (calendar icon)
   - Guests selector
   - Price breakdown (nights × price + fees)
   - "Book Now" button

   INTERACTIVE CALENDAR:
   - table_calendar paket
   - Custom day builders:
     * Available: light gray
     * Selected: accent color
     * Booked: blue
     * Check-in: red top half (Stack + Positioned)
     * Check-out: red bottom half
   - Disable unavailable dates
   - Real-time sync sa Supabase

5. STATE MANAGEMENT:
   - PropertyDetailsNotifier
   - BookingCalendarNotifier
   - Selected dates/guests state
   - PropertyDetailsProvider (fetch by ID)
   - UnitsProvider (fetch units za property)
   - SelectedDatesProvider
   - BookingCalculationProvider (total price)

Implementiraj sve sa error handling i loading states.

6. ANIMATIONS:
   - Hero animation sa property card
   - Parallax scroll effects
   - Smooth image gallery transitions

DELIVERABLES:
- Responsive PropertyDetailsScreen (desktop + mobile)
- Image gallery sa full-screen view
- Units selection component
- Availability calendar integration
- Booking widget (sticky/FAB)
- Reviews section
- Map integration
- All Riverpod providers
