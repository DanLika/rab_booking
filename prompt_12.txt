PROMPT 12: Owner Dashboard - CRUD Operations

Kreiraj Owner Dashboard sa three tabs:

1. `lib/features/owner_dashboard/presentation/screens/owner_dashboard_screen.dart`:

   STRUCTURE:
   - Custom TabBar (Moji Objekti, Kalendar, Rezervacije)
   - Tab 1: Properties list + FAB "Add Property"
   - Tab 2: Master calendar view
   - Tab 3: Bookings list sa filterima

2. PROPERTIES TAB:

   a) Properties List:
      - PropertyListTile widget (image, name, units count, status)
      - Edit/Delete actions
      - "Add Property" FAB

   b) Add/Edit Property Screen:
      - Form sa sva polja (name, description, location, amenities)
      - Image picker (multiple)
      - Image upload progress
      - "Save" button

   c) Units Management:
      - Lista jedinica za property
      - Add/Edit/Delete unit
      - Price management

   TAB 1 - MY PROPERTIES (`properties_list_tab.dart`):

   - List/Grid view toggle
   - Property cards sa:
     * Image, name, location
     * Units count
     * Active bookings count
     * Quick actions: Edit, Delete, View Details
   - FloatingActionButton: "Add New Property"
   - Empty state: "You haven't added any properties yet"

3. PROPERTY CRUD:

   a) `add_property_screen.dart`:
      - Multi-step form (3 steps):
        * Step 1: Basic Info (name, description, location)
        * Step 2: Amenities selection (checkboxes grid)
        * Step 3: Images upload (multi-select)
      - Stepper UI widget
      - Form validation na svakom stepu
      - "Save Draft" i "Publish" buttons

   b) `edit_property_screen.dart`:
      - Same form kao add, pre-populated sa existing data
      - Update functionality

   c) Delete functionality:
      - Confirmation dialog
      - Cascade delete check (ako ima unitove, warning)

4. CALENDAR TAB:

   - Multi-unit calendar view
   - Dropdown za odabir property/unit
   - Sve bookings na jednom mjestu
   - Color-coded za različite statuse

   TAB 2 - MASTER CALENDAR (`master_calendar_tab.dart`):

   - table_calendar paket
   - Show ALL bookings across ALL owner's properties
   - Color-coded by property
   - Filter dropdown: Select specific property
   - Day tap → show bookings detail bottom sheet

5. BOOKINGS TAB:

   - Lista sa filterima (Pending, Confirmed, Completed, Cancelled)
   - Booking card: Guest info, dates, price, status
   - Actions: Confirm/Cancel
   - Search/filter functionality

   TAB 3 - BOOKINGS MANAGEMENT (`bookings_list_tab.dart`):

   - Booking cards sa:
     * Guest name, property/unit
     * Check-in/Check-out dates
     * Status badge (pending, confirmed, completed)
     * Total price
     * Actions: Confirm, Cancel, View Details
   - Filter chips: Status, Property, Date range
   - Search bar: Guest name
   - Sort: Date, Property, Status

6. STATE MANAGEMENT:

   - PropertiesNotifier (CRUD operations)
   - UnitsNotifier
   - OwnerBookingsNotifier
   - OwnerPropertiesProvider (fetch owner's properties)
   - PropertyFormNotifier (add/edit form state)
   - OwnerBookingsProvider (fetch bookings sa filters)
   - DeletePropertyMutation (Riverpod mutation)

7. REPOSITORY:
   Implementiraj sve Supabase operacije sa RLS provjerama.

Svi formi treba da imaju validaciju i error handling.

8. IMAGE UPLOAD:

   - Use image_picker package
   - Upload to Supabase Storage
   - Show upload progress
   - Thumbnail preview
   - Max 10 images per property

9. RESPONSIVE DESIGN:

   - Mobile: Tabs
   - Tablet/Desktop: Sidebar navigation instead of tabs
   - Adaptive grid columns

DELIVERABLES:
- Owner Dashboard screen sa 3 tabs
- Properties list (grid/list view)
- Add/Edit Property multi-step form
- Delete with cascade check
- Master calendar sa all bookings
- Bookings management sa filters
- Image upload to Supabase Storage
- All Riverpod providers
- Responsive layout
