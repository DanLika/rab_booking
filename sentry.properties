# Sentry.io configuration for error tracking
# Source maps upload requires auth token (optional)

defaults.project=bookbed
defaults.org=bookbed

# Sentry server URL
defaults.url=https://sentry.io

# Note: Source maps upload requires Sentry auth token
# Generate at: https://sentry.io/settings/account/api/auth-tokens/
# For now, error tracking works without source maps (just less readable stack traces)
